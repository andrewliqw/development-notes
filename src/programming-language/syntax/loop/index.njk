---
title: loop
subject: Programming Language Syntax
---
{% extends "layout_subject.njk" %}
{% block navigation_links %}
{% include "programming-language/syntax/navigation-links.njk" %}
{% endblock %}
{% block main_content %}
<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th></th>
            <th>Java</th>
            <th>C#</th>
            <th>Python</th>
        </tr>
    </thead>
    <tbody>
        <tr>
            <td>c-like for loop</td>
            <td>for (initialization; condition; update) {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>}</td>
            <td>for (initialization; condition; update) {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>}</td>
            <td>No c-like for loop</td>
        </tr>
        <tr>
            <td>for-each loop</td>
            <td>for (Type variable : iterable) {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>}</td>
            <td>for (Type variable in enumerable) {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>}</td>
            <td>
                for variable in iterable:<br>&nbsp;&nbsp;&nbsp;&nbsp;statements<br>
            </td>
        </tr>
        <tr>
            <td>while loop</td>
            <td>while (condition) {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>}</td>
            <td>while (condition) {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>}</td>
            <td>while condition:<br>&nbsp;&nbsp;&nbsp;&nbsp;statements<br></td>
        </tr>
        <tr>
            <td>do-while loop</td>
            <td>do {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>} while (condition);</td>
            <td>do {<br>&nbsp;&nbsp;&nbsp;&nbsp;statements;<br>} while (condition);</td>
            <td>No do-while loop</td>
        </tr>
        <tr>
            <td>iterator</td>
            <td></td>
            <td>
                Generic:
                <ul>
                    <li>IEnumerable&lt;T&gt; { IEnumerator&lt;T&gt; GetEnumerator(); }</li>
                    <li>IEnumerator&lt;T&gt; { T Current { get; }, bool MoveNext(); void Reset()}</li>
                </ul>
                Non-Generic:
                <ul>
                    <li>IEnumerable { IEnumerator GetEnumerator(); }</li>
                    <li>IEnumerator { Current { get; }, bool MoveNext(); }</li>
                </ul>
                yield return statement will return IEnumerable&lt;T&gt;/IEnumerable or IEnumerator&lt;T&gt;/IEnumerator depending on the context.
            </td>
            <td>
                __iter__()<br>
                __next__()
            </td>
        </tr>
    </tbody>
</table>
{% endblock %}